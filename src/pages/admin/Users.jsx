import React from 'react';
export default function UsersPage(){ return (
  <div>
    <div className="page-header"><h1>Usuarios</h1></div>
    <div className="card">Listado de usuarios (conectar a `/users` en backend)</div>
  </div>
); }
